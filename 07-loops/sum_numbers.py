# Store the numbers that will be added together.
numbers = [5, 10, 15, 20]

# Start the total at 0 before adding any numbers.
total = 0

# Process each number in the list one at a time.
for number in numbers:

    # Add the current number to the running total.
    total += number

# Display the final total.
print(total)